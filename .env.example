# Claude Agent SDK Authentication
# Copy this file to .env and add your credentials
# Then load with: source .env (or use direnv for automatic loading)

# Option 1: API Key (from console.anthropic.com)
# Uncomment and add your API key:
# ANTHROPIC_API_KEY=sk-ant-api03-...

# Option 2: OAuth Token (from `claude setup-token`)
# Uncomment and add your OAuth token:
# CLAUDE_CODE_OAUTH_TOKEN=your-oauth-token-here

# Option 3: 1Password CLI Reference
# Uncomment and add your 1Password reference:
# CLAUDE_CODE_OAUTH_TOKEN="op://Personal/Claude/oauth_token"
# Requires 1Password CLI: https://developer.1password.com/docs/cli

# Note: Only one authentication method is needed
